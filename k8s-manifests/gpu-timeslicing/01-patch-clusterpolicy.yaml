# Patch for ClusterPolicy to Enable Time-Slicing
#
# This is NOT a standard K8s resource - it's a kubectl patch
# Cannot be applied directly with `kubectl apply`
#
# Instead, use one of these methods:
#
# Method 1: kubectl patch (recommended)
#   kubectl patch clusterpolicy/cluster-policy \
#     -n gpu-operator --type merge \
#     -p '{"spec": {"devicePlugin": {"config": {"name": "time-slicing-config", "default": "any"}}}}'
#
# Method 2: kubectl apply with strategic merge patch
#   kubectl apply -f - <<EOF
#   apiVersion: nvidia.com/v1
#   kind: ClusterPolicy
#   metadata:
#     name: cluster-policy
#     namespace: gpu-operator
#   spec:
#     devicePlugin:
#       config:
#         name: time-slicing-config
#         default: any
#   EOF
#
# Method 3: Use the shell script
#   cd ../
#   ./setup-gpu-timeslicing.sh
#
# After patching, the nvidia-device-plugin will restart automatically
# and Kubernetes will report 4 GPUs instead of 1

# This file is for documentation only - see above for actual commands

