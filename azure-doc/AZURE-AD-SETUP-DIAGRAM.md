# Azure AD Authentication Setup - Visual Diagram

## 🎨 What You're Building

```
┌─────────────────────────────────────────────────────────────────────┐
│                        YOUR AZURE AD TENANT                         │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────┐    │
│  │ App Registration: JupyterHub Production                    │    │
│  ├───────────────────────────────────────────────────────────┤    │
│  │ Tenant ID:    12345678-1234-...                           │    │
│  │ Client ID:    87654321-4321-...                           │    │
│  │ Client Secret: AbC123~XyZ789...                           │    │
│  │ Redirect URI:  https://jupyterhub.ccrolabs.com/...        │    │
│  │                                                            │    │
│  │ API Permissions:                                           │    │
│  │ ✓ email, openid, profile, User.Read                       │    │
│  │ ✓ GroupMember.Read.All (for group-based access)           │    │
│  └───────────────────────────────────────────────────────────┘    │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────┐    │
│  │ Security Groups                                            │    │
│  ├───────────────────────────────────────────────────────────┤    │
│  │                                                            │    │
│  │  📁 JupyterHub-Users                                       │    │
│  │     ├─ alice@company.com                                   │    │
│  │     ├─ bob@outlook.com (Guest)                             │    │
│  │     ├─ charlie@company.com                                 │    │
│  │     └─ you@company.com                                     │    │
│  │                                                            │    │
│  │  📁 JupyterHub-Admins                                      │    │
│  │     ├─ you@company.com                                     │    │
│  │     └─ admin@company.com                                   │    │
│  │                                                            │    │
│  └───────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                                 ↓
                    AUTHENTICATION & AUTHORIZATION
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    JUPYTERHUB (values-helm.yaml)                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Authentication: AzureAdOAuthenticator                              │
│                                                                     │
│  Allowed Groups:                                                    │
│    - JupyterHub-Users      → Regular access                         │
│    - JupyterHub-Admins     → Admins need access too!                │
│                                                                     │
│  Admin Groups:                                                      │
│    - JupyterHub-Admins     → Admin privileges                       │
│                                                                     │
│  Admin Access: true         → Admins can access user notebooks      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                 ↓
                         USER EXPERIENCE
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    WHAT USERS SEE                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Regular User (alice@company.com):                                  │
│  ├─ In JupyterHub-Users group ✓                                    │
│  ├─ Can login to JupyterHub ✓                                      │
│  ├─ Can start their own server ✓                                   │
│  ├─ Can access their own notebooks ✓                               │
│  ├─ Cannot see /hub/admin ✗                                        │
│  └─ Cannot access other users' servers ✗                           │
│                                                                     │
│  Admin User (you@company.com):                                      │
│  ├─ In JupyterHub-Users group ✓                                    │
│  ├─ In JupyterHub-Admins group ✓                                   │
│  ├─ Can login to JupyterHub ✓                                      │
│  ├─ Can start their own server ✓                                   │
│  ├─ Can access their own notebooks ✓                               │
│  ├─ Can see /hub/admin panel ✓                                     │
│  ├─ Can see all users and their servers ✓                          │
│  ├─ Can start/stop other users' servers ✓                          │
│  └─ Can access other users' notebooks ✓                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Login Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: User visits https://jupyterhub.ccrolabs.com             │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: JupyterHub redirects to Azure AD login                  │
│         (User sees Microsoft login page)                        │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: User enters credentials                                 │
│         - Email: alice@company.com                              │
│         - Password: ••••••••                                    │
│         - MFA: (if configured)                                  │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Azure AD validates credentials                          │
│         ✓ User authenticated                                    │
│         ✓ Returns user info + group memberships                 │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Azure AD redirects back to JupyterHub                   │
│         https://jupyterhub.ccrolabs.com/hub/oauth_callback      │
│         (with authorization code)                               │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 6: JupyterHub receives user data                           │
│         - Email: alice@company.com                              │
│         - Groups: [JupyterHub-Users]                            │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 7: JupyterHub checks authorization                         │
│                                                                 │
│  ❓ Is alice in allowed_groups?                                 │
│     - JupyterHub-Users? ✓ YES                                   │
│     - JupyterHub-Admins? ✗ NO                                   │
│     → GRANT ACCESS                                              │
│                                                                 │
│  ❓ Is alice in admin_groups?                                   │
│     - JupyterHub-Admins? ✗ NO                                   │
│     → NO ADMIN PRIVILEGES                                       │
│                                                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 8: Alice lands on JupyterHub home page                     │
│         - Can choose CPU or GPU profile                         │
│         - Can start server                                      │
│         - No admin panel link shown                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 👥 Group Membership Matrix

```
┌─────────────────┬──────────────────┬──────────────────┬────────────┐
│ User            │ JupyterHub-Users │ JupyterHub-Admins│ Result     │
├─────────────────┼──────────────────┼──────────────────┼────────────┤
│ alice@company   │       ✓          │        ✗         │ Regular    │
│ bob@outlook     │       ✓          │        ✗         │ Regular    │
│ you@company     │       ✓          │        ✓         │ Admin      │
│ admin@company   │       ✓          │        ✓         │ Admin      │
│ charlie@company │       ✗          │        ✗         │ No Access  │
│ hacker@evil.com │       ✗          │        ✗         │ No Access  │
└─────────────────┴──────────────────┴──────────────────┴────────────┘

Key:
✓ = Member of group
✗ = Not member of group

Access Rules:
- Must be in JupyterHub-Users OR JupyterHub-Admins to login
- Must be in JupyterHub-Admins to have admin privileges
- Admins should be in BOTH groups (Users + Admins)
```

---

## 🔐 API Permissions Flow

```
When user logs in, Azure AD provides this info to JupyterHub:

┌─────────────────────────────────────────────────────────────────┐
│ API Permission        │ What JupyterHub Gets                    │
├───────────────────────┼─────────────────────────────────────────┤
│ openid                │ Basic authentication                    │
│ profile               │ Display name: "Alice Johnson"           │
│ email                 │ Email: "alice@company.com"              │
│ User.Read             │ User info from Azure AD                 │
│ GroupMember.Read.All  │ Groups: ["JupyterHub-Users"]            │
└───────────────────────┴─────────────────────────────────────────┘

Without GroupMember.Read.All:
  ✗ JupyterHub can't see group memberships
  ✗ Group-based access control won't work
  ✓ Email-based access control still works
```

---

## 🎯 Access Control Decision Tree

```
User tries to login
        ↓
    ┌───────┐
    │ Authenticate │
    │ with Azure AD│
    └───┬───┘
        ↓
        ✓ Valid Azure AD user
        ↓
    ┌─────────────────────┐
    │ Check allowed_groups│
    └───┬─────────────────┘
        ↓
    In JupyterHub-Users? ────────┐
        ↓ YES                     │
    In JupyterHub-Admins? ───┐   │
        ↓ YES                 │   │ NO
        │                     │   │
        ↓                     ↓   ↓
    ┌────────┐          ┌────────┐
    │ GRANT  │          │ DENY   │
    │ ACCESS │          │ ACCESS │
    └───┬────┘          └────────┘
        ↓                    ↓
    Check admin_groups   Show error:
        ↓               "Not authorized"
    In JupyterHub-Admins?
        ↓ YES      ↓ NO
        │          │
        ↓          ↓
    ┌────────┐  ┌────────────┐
    │ ADMIN  │  │ REGULAR    │
    │ RIGHTS │  │ USER       │
    └────────┘  └────────────┘
        ↓             ↓
    Can access    Can access
    /hub/admin    only own server
```

---

## 📊 Configuration Placeholders → Actual Values

### Before (What you have now):

```python
# In helm/values-helm.yaml

c.AzureAdOAuthenticator.tenant_id = "YOUR_TENANT_ID_HERE"
c.AzureAdOAuthenticator.client_id = "YOUR_CLIENT_ID_HERE"
c.AzureAdOAuthenticator.client_secret = "YOUR_CLIENT_SECRET_HERE"

c.AzureAdOAuthenticator.allowed_groups = {
    "JupyterHub-Users",
    "JupyterHub-Admins"
}

c.AzureAdOAuthenticator.admin_groups = {
    "JupyterHub-Admins"
}
```

### After (What you'll have):

```python
# In helm/values-helm.yaml

c.AzureAdOAuthenticator.tenant_id = "12345678-1234-1234-1234-123456789abc"
c.AzureAdOAuthenticator.client_id = "87654321-4321-4321-4321-cba987654321"
c.AzureAdOAuthenticator.client_secret = "AbC123~XyZ789_defGHI456-jklMNO789"

c.AzureAdOAuthenticator.allowed_groups = {
    "JupyterHub-Users",      # ← Match Azure AD group name exactly!
    "JupyterHub-Admins"      # ← Match Azure AD group name exactly!
}

c.AzureAdOAuthenticator.admin_groups = {
    "JupyterHub-Admins"      # ← Match Azure AD group name exactly!
}
```

---

## 🚀 Deployment Flow

```
┌──────────────────────────────────────────────────────────────┐
│ Phase 1: Azure AD Setup (NOW - No subscription needed!)     │
├──────────────────────────────────────────────────────────────┤
│ 1. Create app registration                                   │
│ 2. Get credentials (Tenant ID, Client ID, Secret)            │
│ 3. Configure API permissions                                 │
│ 4. Grant admin consent                                       │
│ 5. Create groups (JupyterHub-Users, JupyterHub-Admins)       │
│ 6. Add users to groups                                       │
└────────────────────────┬─────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────────┐
│ Phase 2: Configuration (NOW - No subscription needed!)       │
├──────────────────────────────────────────────────────────────┤
│ 1. Update values-helm.yaml with credentials                  │
│ 2. Verify group names match exactly                          │
│ 3. (Optional) Test locally with Docker Compose              │
└────────────────────────┬─────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────────┐
│ Phase 3: Deployment (LATER - Needs subscription!)            │
├──────────────────────────────────────────────────────────────┤
│ 1. Get Azure subscription access                             │
│ 2. Deploy JupyterHub to AKS                                  │
│ 3. Test authentication                                       │
│ 4. Verify admin access                                       │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Management Workflow

### Adding New Users (After Deployment)

```
Option A: Internal User
┌─────────────────────────────────────┐
│ Azure AD → Groups → JupyterHub-Users│
│ → Members → + Add members            │
│ → Search user → Select → Done       │
└────────────────┬────────────────────┘
                 ↓
        User can login immediately!
        (No JupyterHub redeploy needed!)


Option B: External User (Guest)
┌─────────────────────────────────────┐
│ Azure AD → Users → Invite external  │
│ → Email: user@outlook.com           │
│ → Groups: [JupyterHub-Users]        │
│ → Invite                            │
└────────────────┬────────────────────┘
                 ↓
        User receives email
                 ↓
        User accepts invitation
                 ↓
        User can login!
```

### Making Someone Admin (After Deployment)

```
┌─────────────────────────────────────────┐
│ Azure AD → Groups → JupyterHub-Admins   │
│ → Members → + Add members               │
│ → Search user → Select → Done           │
└────────────────┬────────────────────────┘
                 ↓
        User has admin rights on next login!
        (No JupyterHub redeploy needed!)
```

### Removing Access (After Deployment)

```
┌─────────────────────────────────────────┐
│ Azure AD → Groups → JupyterHub-Users    │
│ → Members → Select user → Remove        │
└────────────────┬────────────────────────┘
                 ↓
        User loses access on next login
        (Active session continues until timeout)
```

---

## 📝 What Makes This Powerful

### ✅ Dynamic Access Management

```
┌──────────────────────────────────────────────────────────────┐
│ Email-Based (Old Way)                                        │
├──────────────────────────────────────────────────────────────┤
│ Add user → Edit values-helm.yaml → Redeploy → User can login│
│ Time: 10-15 minutes per user                                 │
│ Requires: Deployment access, kubectl access                  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ Group-Based (New Way) ✨                                     │
├──────────────────────────────────────────────────────────────┤
│ Add user → Add to Azure AD group → User can login           │
│ Time: 30 seconds per user                                    │
│ Requires: Azure AD access only (no deployment needed!)      │
└──────────────────────────────────────────────────────────────┘
```

### ✅ Centralized Management

```
All your apps can use the same Azure AD groups:
├─ JupyterHub (uses JupyterHub-Users group)
├─ Internal Dashboard (uses JupyterHub-Users group)
├─ Data Lake Access (uses JupyterHub-Users group)
└─ MLflow Server (uses JupyterHub-Users group)

Add user to group once → Access to all apps! ✨
```

---

## 🎯 Next Steps

**You are here**: 📍 Phase 1 - Azure AD Setup

**Next action**: Follow `SETUP-AZURE-APP-REGISTRATION.md` step-by-step!

**Quick checklist**:
1. ⏳ Create app registration (5 min)
2. ⏳ Copy credentials (2 min)
3. ⏳ Configure API permissions (3 min)
4. ⏳ Create groups (5 min)
5. ⏳ Update values-helm.yaml (2 min)
6. ⏸️ Wait for Azure subscription
7. ⏸️ Deploy JupyterHub

**You can complete steps 1-5 RIGHT NOW!** 🚀

